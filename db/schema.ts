import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const bookings=sqliteTable('bookings',{id:text('id').primaryKey(),requestId:text('request_id').notNull(),reference:text('reference').notNull(),test:text('test').notNull(),category:text('category').notNull(),date:text('date').notNull(),time:text('time').notNull(),location:text('location').notNull(),contact:text('contact').notNull(),company:text('company').notNull(),phone:text('phone').notNull(),email:text('email').notNull(),vehicle:text('vehicle').notNull(),status:text('status').notNull().default('Pending'),reason:text('reason').notNull().default(''),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull()},t=>[uniqueIndex('bookings_request_unique').on(t.requestId),uniqueIndex('bookings_reference_unique').on(t.reference),index('bookings_date_status').on(t.date,t.status)]);
export const settings=sqliteTable('settings',{id:integer('id').primaryKey(),mode:text('mode').notNull().default('unconfigured'),anchor:text('anchor').notNull().default('')});
export const blocks=sqliteTable('blocks',{date:text('date').primaryKey(),reason:text('reason').notNull().default('')});
export const outbox=sqliteTable('outbox',{id:text('id').primaryKey(),bookingId:text('booking_id').notNull().references(()=>bookings.id),kind:text('kind').notNull(),status:text('status').notNull().default('queued'),attempts:integer('attempts').notNull().default(0),error:text('error').notNull().default(''),createdAt:text('created_at').notNull(),sentAt:text('sent_at'),leaseUntil:integer('lease_until').notNull().default(0)},t=>[uniqueIndex('outbox_booking_kind').on(t.bookingId,t.kind),index('outbox_status').on(t.status)]);
export const rateLimits=sqliteTable('rate_limits',{key:text('key').primaryKey(),count:integer('count').notNull(),expires:integer('expires').notNull()});
